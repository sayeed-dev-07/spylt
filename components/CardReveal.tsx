import React from 'react';

interface prop{
    text: string,
    white?: boolean,
    bgColor:string
}

const CardReveal = ({text, white=true, bgColor}:prop) => {
    return (
        <div>
            
        </div>
    );
};

export default CardReveal;